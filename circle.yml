machine:
  services:
    - docker
  ruby:
    version: 2.2.4
dependencies:
  cache_directories:
    - /opt/circleci/.rvm/rubies/ruby-2.2.3
    - /opt/circleci/.rvm/gems/ruby-2.2.4
test:
  override:
    - bundle exec rake style
    - bundle exec rake spec:chef
    - bundle exec rake integration:kitchen:all
